#import "../../format/lib.typ":*
#import "../../format/utils.typ":*

== 万有引力与天体动力学

=== 万有引力的有心力本质

万有引力是一种典型的*有心力*，即力始终指向固定的力心，且大小仅取决于距离 $r$ . 这一本质导致了天体运动中两个关键的守恒量：*角动量*与*机械能* . 

==== 角动量守恒与面积定律
在有心力场中，力矩 $vec(M) = vec(r) times vec(F) = 0$，故角动量 $vec(L)$ 恒定 . 
在极坐标系 $(r, theta)$ 中，速度 $vec(v) = dot(r) vec(e)_r + r dot(theta) vec(e)_theta$ . 
角动量大小为：
$ L = m r (r dot(theta)) = m r^2 dot(theta) = "const" $
行星在 $dif t$ 时间内扫过的面积 $dif S = 1/2 r^2 dif theta$ . 
由此得*面速度*：
$ (dif S) / (dif t) = 1/2 r^2 dot(theta) = L / (2 m) = "const" $
这就是开普勒第二定律的严格数学推导：它是有心力场中角动量守恒的几何表现 . 

=== 轨道微分方程与开普勒第一定律

#theorem(title: "比内 (Binet) 方程的推导")[
  我们希望求得轨道方程 $r(theta)$ . 令 $u = 1/r$，利用角动量守恒 $dot(theta) = L u^2 / m$ . 
  对时间求导转化为对角度求导：
  $ dot(r) = dif / (dif t) (1/u) = - 1/u^2 (dif u) / (dif theta) dot(theta) = - L/m (dif u) / (dif theta) $
  再次求导：
  $ ddot(r) = dif / (dif t) (- L/m (dif u) / (dif theta)) = - L^2 u^2 / m^2 (dif^2 u) / (dif theta^2) $
  代入径向动力学方程 $m(ddot(r) - r dot(theta)^2) = - G M m u^2$：
  $ - L^2 u^2 / m (dif^2 u) / (dif theta^2) - L^2 u^3 / m = - G M m u^2 $
  整理得*轨道微分方程*：
  $ (dif^2 u) / (dif theta^2) + u = (G M m^2) / L^2 $
]

这是一个非齐次线性常微分方程，其形式类似于简谐振动 . 其通解为：
$ u = 1/r = (G M m^2) / L^2 (1 + e cos theta) arrow.long r = p / (1 + e cos theta) $
其中 $p = L^2 / (G M m^2)$ 为半正焦轴，$e$ 为离心率 . 结论：天体轨道必为二次曲线，这证明了开普勒第一定律 . 

=== 能量视角：为什么能量决定轨道形状？

系统的总机械能 $E$ 包含动能与引力势能 . 在极坐标下：
$ E = 1/2 m dot(r)^2 + 1/2 m r^2 dot(theta)^2 - (G M m) / r $
利用 $L = m r^2 dot(theta)$，将动能项中的转动部分提取出来，定义*有效势能* $V_(e f f)$：
$ E = 1/2 m dot(r)^2 + ( L^2 / (2 m r^2) - (G M m) / r ) = 1/2 m dot(r)^2 + V_(e f f)(r) $

- *离心力势能项* $L^2 / (2 m r^2)$ 在 $r arrow 0$ 时趋于无穷，防止物体撞向中心 . 
- *引力势能项* $- (G M m) / r$ 在 $r arrow infinity$ 时趋于 $0$ . 

#theorem(title: "离心率与能量的关系")[
  通过代入轨道方程解，可以推导出离心率 $e$ 与总能量 $E$ 的关系：
  $ e = sqrt(1 + (2 E L^2) / (m (G M m)^2)) $
]

1. *束缚态 ($E < 0$)*：$e < 1$，轨道为*椭圆* . 
2. *临界态 ($E = 0$)*：$e = 1$，轨道为*抛物线* . 
3. *逃逸态 ($E > 0$)*：$e > 1$，轨道为*双曲线* . 

=== 变轨问题：霍曼转移轨道 (Hohmann Transfer)

变轨的核心在于利用*活力公式 (Vis-viva equation)*：$v^2 = G M (2/r - 1/a)$ . 

- *从圆轨道 1 转移到圆轨道 2*

  设两圆轨道半径分别为 $r_1, r_2$ ($r_1 < r_2$) . 转移轨道的半长轴为 $a = (r_1 + r_2) / 2$ . 
  
  1. *第一次加速*：在 $r_1$ 处由圆轨道速度 $v_(c 1) = sqrt(G M / r_1)$ 增加到转移轨道近地点速度 $v_p$：
     $ v_p = sqrt(G M (2 / r_1 - 1 / a)) = sqrt(G M ( (2 r_2) / (r_1 (r_1 + r_2)) )) $

     
  2. *第二次加速*：到达远地点 $r_2$ 时，速度为 $v_a$，需增加到圆轨道速度 $v_(c 2) = sqrt(G M / r_2)$：
     $ v_a = sqrt(G M (2 / r_2 - 1 / a)) = sqrt(G M ( (2 r_1) / (r_2 (r_1 + r_2)) )) $



=== 开普勒第三定律的普适推导

对于椭圆轨道，在一个周期 $T$ 内扫过的面积为整个椭圆面积 $S = pi a b$ . 
已知面速度 $dif S / dif t = L / (2m)$，故：
$ T = S / (L / (2 m)) = (2 m pi a b) / L $
利用椭圆几何关系 $b^2 = a^2 (1 - e^2)$ 和轨道参数 $p = a(1 - e^2) = L^2 / (G M m^2)$：
$ T^2 = (4 m^2 pi^2 a^2 b^2) / L^2 = (4 m^2 pi^2 a^4 (1-e^2)) / (G M m^2 a (1-e^2)) = (4 pi^2) / (G M) a^3 $
由此得 $a^3 / T^2 = (G M) / (4 pi^2)$ . 这一推导证明了比例系数只与中心天体质量有关 . 

#pagebreak()

#example(title: "双天体系的潮汐效应与锁定分析")[
    
  质量分别为 $M$、$m$、半径分别为 $R_1$、$R_2$ 的匀质天体绕它们的质心作角速度为 $omega$ 的圆周运动。$m$ 以角速度 $omega_s$ 自转（$omega_s$ 方向同 $omega$），$M$ 无自转。已知 $M$、$m$ 中心相距 $R$，$R >> R_2$。

  (1) 由于潮汐效应，系统总能量会随时间缓慢减小，$omega_s$ 与 $omega$ 会缓慢改变。求初始状态满足什么条件时（用初始 $R$ 满足的不等式表示），潮汐效应会使得时间足够长时，$omega_s = omega$。

  (2) 有模型描述如下：地月系统形成时，地球质量为 $5.97 times 10^24 upright(k g)$，月球质量为 $7.34 times 10^22 upright(k g)$，月心距地心 $38$ 万公里，月球的角速度和当今地球自转角速度接近。结合天文常识，试估计月球形成时自转角动量的最大值。

]

#pagebreak()