#import "../../format/lib.typ":*
#import "../../format/utils.typ":*

== 刚体动力学

刚体是指在任何力作用下，内部任意两点间的距离始终保持不变的理想物理模型 . 这一约束意味着刚体只有平动和转动两种基本运动形式 . 

=== 刚体运动学描述

==== 速度分解与基点法

刚体上任意两点 $O$ 和 $P$ 的位移关系受到刚体不形变的约束 . 设 $O$ 为参考基点，则 $P$ 点的速度可表示为：
$ vec(v)_P = vec(v)_O + vec(v)_(P O) $
由于 $O, P$ 间距不变，$P$ 相对 $O$ 只能做圆周运动，故有：
$ vec(v)_P = vec(v)_O + vec(omega) times vec(r)_(O P) $
其中 $vec(omega)$ 为刚体转动的角速度矢量 . 这一公式表明：刚体上任一点的速度等于随基点的平动速度与绕基点转动速度的矢量和 . 

==== 瞬时转动中心（瞬心）

#theorem(title: "瞬心定理")[
  对于做平面运动的刚体，在任一瞬时，总能找到一个速度为零的点（或在无穷远处），称为*瞬时转动中心*（简称瞬心 $C$） . 此时，刚体上各点的速度分布等效于绕该点 $C$ 的纯转动：
  $ vec(v)_P = vec(omega) times vec(r)_(C P) $
]

- *寻找方法*：已知刚体上两点 $A, B$ 的速度方向，分别作 $vec(v)_A$ 和 $vec(v)_B$ 的垂线，其交点即为瞬心 . 这一方法在处理复杂机械联动问题时具有极高的几何简洁性 . 

=== 转动惯量

转动惯量 $I$ 是描述刚体转动惯性大小的物理量，其地位对偶于平动中的质量 $m$ . 

#theorem(title: "转动惯量的计算")[
  对于连续分布的刚体，转动惯量的定义式为：
  $ I = integral r^2 dif m $
  其中 $r$ 是质量微元 $dif m$ 到转动轴的垂直距离 . 
]

- *平行轴定理*：
  若已知刚体绕过质心轴的转动惯量为 $I_c$，则绕与之平行且距离为 $d$ 的新轴的转动惯量为：
  $ I = I_c + m d^2 $
  *证明步奏*：
  设质心为原点，新轴位置为 $(-d, 0)$ . 
  $ I = integral ((x+d)^2 + y^2) dif m = integral (x^2 + y^2 + 2x d + d^2) dif m $
  $ I = integral (x^2 + y^2) dif m + d^2 integral dif m + 2d integral x dif m $
  由于原点在质心，故 $integral x dif m = 0$，得 $I = I_c + m d^2$ . 

=== 常见均匀刚体的转动惯量表

#align(center)[
  #table(
    columns: (auto, 1fr, 1fr),
    inset: 10pt,
    align: horizon,
    fill: (x, y) => if y == 0 { gray.lighten(80%) },
    [*刚体形状*], [*转动轴位置*], [*转动惯量 $I$*],
    [细杆 (长 $L$)], [过中心且垂直于杆], [$1 / 12 m L^2$],
    [细杆 (长 $L$)], [过一端且垂直于杆], [$1 / 3 m L^2$],
    [圆环 (半径 $R$)], [过圆心且垂直于环面], [$m R^2$],
    [圆环 (半径 $R$)], [沿环的任一直径], [$1 / 2 m R^2$],
    [圆柱体/圆盘 (半径 $R$)], [通过中心轴线], [$1 / 2 m R^2$],
    [薄圆板 (半径 $R$)], [沿板的任一直径], [$1 / 4 m R^2$],
    [空心圆柱 (半径 $R_1, R_2$)], [通过中心轴线], [$1 / 2 m (R_1^2 + R_2^2)$],
    [均匀球壳 (半径 $R$)], [通过球心的任一轴], [$2 / 3 m R^2$],
    [均匀实心球 (半径 $R$)], [通过球心的任一轴], [$2 / 5 m R^2$],
    [矩形薄板 (边长 $a, b$)], [过中心且垂直于板面], [$1 / 12 m (a^2 + b^2)$],
  )
]

#remark()[
  注意区分：
  1. 对于同一个刚体，转动轴位置不同，转动惯量完全不同 . 

  2. 记住圆盘中心的 $1 / 2 m R^2$ 和实心球心的 $2 / 5 m R^2$ 是解决大部分滚动问题的基础 . 
  3. 对于薄板模型，利用正交轴定理 $I_z = I_x + I_y$ 可以快速从直径转动惯量推导出中心轴转动惯量 . 
]

=== 定轴转动动力学

==== 转动定律的数学推导

对于绕固定轴 $z$ 转动的刚体，考虑其中一个质量元 $m_i$ . 其受到的切向力为 $F_(i tau)$，根据牛顿第二定律：
$ F_(i tau) = m_i a_(i tau) = m_i (r_i alpha) $
两边同时叉乘 $r_i$ 得到力矩：
$ M_i = r_i F_(i tau) = m_i r_i^2 alpha $
对整个刚体求和，由于内力矩之和为零，得：
$ sum M_(e x t) = (sum m_i r_i^2) alpha arrow.long M = I alpha $

=== 刚体平面运动的能量

==== 柯尼希定理（Koenig's Theorem）

刚体做一般平面运动时的动能可以分解为两部分：随质心的平动动能和绕质心的转动动能 . 

#theorem(title: "动能分解公式")[
  $ E_k = 1/2 m v_c^2 + 1/2 I_c omega^2 $
]

*推导过程*：
刚体上任一点速度 $vec(v)_i = vec(v)_c + vec(v)'_i$，其中 $vec(v)'_i$ 为相对质心的速度 . 
$ E_k = sum 1/2 m_i vec(v)_i^2 = sum 1/2 m_i (vec(v)_c + vec(v)'_i) dot (vec(v)_c + vec(v)'_i) $
$ E_k = 1/2 (sum m_i) v_c^2 + 1/2 sum m_i v_i^('2) + vec(v)_c dot (sum m_i vec(v)'_i) $
由于在质心系中 $sum m_i vec(v)'_i = 0$，且 $sum m_i v_i^('2) = sum m_i (r_i omega)^2 = I_c omega^2$ . 
故得 $E_k = 1/2 m v_c^2 + 1/2 I_c omega^2$ . 

=== 角动量守恒

对于刚体，角动量定义为 $vec(L) = I vec(omega)$ . 根据力矩与角动量的关系 $vec(M) = (dif vec(L)) / (dif t)$，可以导出：

#theorem(title: "角动量守恒定律")[
  当系统所受合外力矩为零时（$sum vec(M)_(e x t) = 0$），系统的总角动量保持不变 . 
  $ I_1 omega_1 = I_2 omega_2 $
]

#pagebreak()

// 例 63
#example(title: "纯滚动圆盘与铰接细杆的联动分析")[
  
  如图所示，在足够粗糙的导轨上放置有半径为 $R$、质量为 $M$ 的匀质圆盘，圆盘只能在导轨上纯滚动。一根长度为 $2L$、质量为 $m$ 的杆与圆盘光滑铰接，连接点在圆盘圆心 $O$ 处。将圆盘的质心速度记为 $v$，杆与竖直方向的夹角为 $theta$，杆的角速度为 $omega$。初始时，杆与竖直方向的夹角 $theta_0 = pi / 2$，角速度 $omega_0 = 0$，圆盘的质心速度 $v_0 = 0$。将杆释放，系统开始运动。

  (1) 请证明：在系统的运动过程中，$P_x = (3 / 2 M + m)v + m L omega cos theta$ 是一个守恒量。

  (2) 杆与竖直方向的夹角为 $theta$ 时，推导圆盘的质心速度、杆的角速度的表达式。

  (3) 依靠外力使圆盘停止滚动，待系统稳定后，在 $theta = 0$ 附近给予杆一个小扰动，求系统小振动的周期。

  #align(right)[
    #block(width: 40%)[
      #figure(
        image("image/方圆联考3.png", width: 100%),
      )
    ]
  ]
]

#pagebreak()