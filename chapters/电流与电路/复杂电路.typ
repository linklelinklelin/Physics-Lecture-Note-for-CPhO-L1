#import "../../format/lib.typ":*
#import "../../format/utils.typ":*

== 复杂电路

=== 基尔霍夫定律：电路分析的两大支柱

基尔霍夫定律是电荷守恒和能量守恒在电路中的直接体现 . 

#theorem(title: "基尔霍夫第一定律 (KCL)")[
  *节点电流定律*：对于电路中的任一节点，流入电流的代数和恒等于零 . 
  $ sum_{k} I_k = 0 $
]

#theorem(title: "基尔霍夫第二定律 (KVL)")[
  *回路电压定律*：沿任一闭合回路绕行一周，各支路电压的代数和恒等于零 . 
  $ sum U_k = 0 quad "或" quad sum cal(E) = sum I R $
]

=== 系统化解题算法

当电路支路过多时，列写联立方程组需要技巧以减少未知数 . 

1. *节点电压法 (Node Voltage Method)*：
   以电位为基础 . 选定一个基准点（接地），以其他节点电位为未知数，对每个独立节点列 KCL 方程 . 
2. *回路电流法 (Mesh Current Method)*：
   以电流为基础 . 为每个独立回路假设一个循环电流，根据 KVL 列方程 . 

#example(title: "利用节点电压法求解多电源电路")[
]


=== 线性网络的特殊性质

线性电路（即电阻、电源不随电流变化的电路）具有非常优美的对称性与比例性 . 

#theorem(title: "叠加定理 (Superposition Theorem)")[
  在含有多个独立电源的线性电路中，任一支路的电流（或电压）等于各电源单独作用时在该支路产生的电流（或电压）的代数和 . 
]
- *注意*：当某个电源“不作用”时，电压源应视为*短路*（电压为 $0$），电流源应视为*断路*（电流为 $0$） . 

#theorem(title: "替代定理 (Substitution Theorem)")[
  如果已知线性电路中某条支路的电压为 $U$、电流为 $I$，那么该支路可以用一个电压等于 $U$ 的电压源，或电流等于 $I$ 的电流源，或阻值等于 $U/I$ 的电阻来替换，而不改变电路其余部分的电流和电压分布 . 
]

=== 戴维南定理与诺顿定理

这是简化复杂二端网络的最强武器 . 

#theorem(title: "戴维南定理 (Thévenin's Theorem)")[
  任何含源线性二端网络，对外电路而言，都可以等效为一个电压源 $cal(E)_"eq"$ 与一个电阻 $R_i$ 的串联 . 
  - $cal(E)_"eq"$ 等于网络的*开路电压* . 
  - $R_i$ 等于网络内电源置零后的*等效输入电阻* . 
]

#theorem(title: "诺顿定理 (Norton's Theorem)")[
  任何含源线性二端网络，对外电路而言，都可以等效为一个电流源 $I_s$ 与一个电阻 $R_i$ 的并联 . 
  - $I_s$ 等于网络的*短路电流* . 
  - $R_i$ 与戴维南等效电阻相同 . 
]

#remark()[
  戴维南与诺顿定理告诉我们：无论内部结构多复杂，只要我们只关心外电路，就可以将其视为一个带内阻的简单电池 . 
]

#example(title: "利用戴维南定理简化复杂桥路计算")[
]

=== 电阻网络的拓扑变换与对称性

在处理某些复杂网络时，我们会发现一些电阻既不是串联也不是并联（例如桥式电路的五个电阻） . 此时，我们需要引入拓扑等效变换来改变电路的结构，使其能够被常规方法求解 . 

==== 星形-三角形变换 (Y-Δ Transform)

#definition(title: "Y-Δ 变换")[
  对于一个具有三个端点 $1, 2, 3$ 的电阻网络：
  1. *星形 (Y 或 T) 联接*：三个电阻 $R_1, R_2, R_3$ 一端连接在公共节点，另一端分别接至端点 . 
  2. *三角形 (Δ 或 π) 联接*：三个电阻 $R_{12}, R_{23}, R_{31}$ 分别连接在三个端点之间 . 
  如果两套网络在任意两个端点之间的等效电阻完全相同，则称它们是*等效*的 . 
]

- *变换公式的导出逻辑*：
  考虑端点 $1$ 和 $2$ 之间的电阻 . 
  - 在 $Y$ 型中，若 $3$ 端开路，$R_{12(Y)} = R_1 + R_2$ . 
  - 在 $Delta$ 型中，$R_{12(Delta)} = R_{12} || (R_{23} + R_{31})$ . 
  联立三个端点对的等效方程组，可以解得：

#theorem(title: "Y-Δ 变换公式")[
  1. *Δ 变 Y (降阻变换)*：每个星形电阻等于相邻三角形电阻的乘积除以三角形电阻之和 . 
     $ R_1 = (R_{12} R_{31}) / (R_{12} + R_{23} + R_{31}) $
  2. *Y 变 Δ (升阻变换)*：每个三角形电阻等于星形电阻两两乘积之和除以对角的星形电阻 . 
     $ R_{12} = (R_1 R_2 + R_2 R_3 + R_3 R_1) / R_3 $
]

- *特殊情况*：若三个电阻相等（对称），则有 $R_Delta = 3 R_Y$ . 

==== 平衡电桥

电桥电路是精密测量和传感器技术的基础 . 

#definition(title: "惠斯通电桥 (Wheatstone Bridge)")[
  由四个电阻 $R_1, R_2, R_3, R_4$ 组成的环路，在其中一对角点接电源，另一对角点接检流计（或中桥电阻 $R_g$）的电路结构 . 
]

- *平衡状态的判定*：
  当检流计中的电流 $I_g$ 为零时，称电桥达到*平衡* . 这意味着两个中点支路的电位相等（$V_C = V_D$） . 

#theorem(title: "电桥平衡条件")[
  电桥平衡的充要条件是：*对臂电阻的乘积相等* . 
  $ R_1 R_4 = R_2 R_3 arrow.long R_1 / R_2 = R_3 / R_4 $
]

- *平衡电桥的性质与应用*：
  1. *测量意义*：若已知 $R_1, R_2, R_3$，可以通过调节使其平衡来精确测量未知电阻 $R_4$ . 
  2. *分析意义*：当电桥平衡时，中桥电阻 $R_g$ 对电路没有任何影响 . 我们既可以将 $R_g$ 视为*断路*（切断支路），也可以将其视为*短路*（用导线直接连接）来简化计算 . 

#remark()[
  如果电桥不平衡，我们无法直接用串并联公式求解 . 此时，最快的方法通常是选取电桥的一半（一个三角形或一个星形）进行 *Y-Δ 变换*，将其转化为简单的串并联电路 . 
]

#pagebreak()